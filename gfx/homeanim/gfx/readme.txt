/gfx/homeanim/gfx/
This folder exists for legacy reasons, to support the old style small slide show that was the default for new installations. Now the default is the larger slideshow,
and the parent folder contains the larger images required for this.

